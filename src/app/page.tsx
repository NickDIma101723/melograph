import Hero from "../components/Hero";

export default function Home() {
  return (
    <main style={{ height: '100dvh', width: '100vw', overflow: 'hidden', backgroundColor: '#000' }}>
      <Hero />
    </main>
  );
}
